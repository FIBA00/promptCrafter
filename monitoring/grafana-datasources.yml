# /monitoring/grafana-datasources.yml
apiVersion: 1

datasources:
    - name: Prometheus
      type: prometheus
      access: proxy
      # The URL points to the prometheus service within the Docker network
      url: http://prometheus:9090
      isDefault: true
      editable: false
